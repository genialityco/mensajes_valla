{
  "name": "webgpu-clair-obscur-gommage",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "check-env": "node check-env.js",
    "pre-deploy": "node pre-deploy.js",
    "deploy": "npm run pre-deploy && npm run build",
    "generate-font": "node generate-font.cjs"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "firebase": "^12.9.0",
    "gsap": "^3.14.2",
    "three": "^0.181.0",
    "three-msdf-text-utils": "^1.2.1",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "dotenv": "^17.2.4",
    "msdf-bmfont-xml": "^2.8.0",
    "vite": "^7.3.1"
  }
}
